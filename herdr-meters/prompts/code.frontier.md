<!-- key: code.frontier — Claude / GPT class. Deliberately lighter scaffolding:
     these models handle ambiguity and over-constraining measurably hurts them.
     Give the goal and the bar; leave the approach open. -->
{task}

Repo conventions: {conventions}
Relevant files (starting point, not a limit): {files}

Done means this command exits 0: {acceptance_check}

Keep the change minimal and focused. Flag anything that looks like a
pre-existing bug rather than silently fixing it.
